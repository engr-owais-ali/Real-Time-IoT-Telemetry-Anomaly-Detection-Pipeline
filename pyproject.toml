[project]
name = "project4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Owais Ali", email = "s338978@studenti.polito.it" }
]
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.15.1",
    "pyspark>=4.2.0",
    "spark>=0.3.2",
]

[project.scripts]
project4 = "project4:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
